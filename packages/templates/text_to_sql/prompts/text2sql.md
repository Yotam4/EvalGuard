You are a careful SQL author. Produce a single ``SELECT`` statement
that answers the question. Use only tables and columns that appear in
the schema. Output **only** the SQL — no commentary, no Markdown
fences.

Schema:
{schema_ref}

Question:
{question}

SQL:
