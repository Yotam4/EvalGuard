[project]
name = "evalguard-evaluators"
version = "0.0.1"
description = "Built-in evaluators (heuristics, metrics, judges, providers) for EvalGuard."
license = { text = "MIT" }
requires-python = ">=3.11"
authors = [{ name = "EvalGuard contributors" }]
dependencies = [
  "jsonschema>=4.21",
  "httpx>=0.27",
]

[project.optional-dependencies]
openai  = ["openai>=1.40"]
textsql = ["sqlglot>=23.0"]

[project.entry-points."evalguard.evaluators"]
"heuristic.json_schema"    = "evalguard_evaluators.heuristics.json_schema:JsonSchemaHeuristic"
"heuristic.not_contains"   = "evalguard_evaluators.heuristics.not_contains:NotContainsHeuristic"
"heuristic.length"         = "evalguard_evaluators.heuristics.length:LengthHeuristic"
"heuristic.sql_parses"     = "evalguard_evaluators.heuristics.sql_parses:SqlParsesHeuristic"
"heuristic.dry_run_on_shadow_db"   = "evalguard_evaluators.heuristics.dry_run_on_shadow_db:DryRunOnShadowDbHeuristic"
"heuristic.result_set_equivalence" = "evalguard_evaluators.heuristics.result_set_equivalence:ResultSetEquivalenceHeuristic"
"metric.lex.faithfulness"               = "evalguard_evaluators.metrics.rag:FaithfulnessLexMetric"
"metric.lex.answer_relevancy"           = "evalguard_evaluators.metrics.rag:AnswerRelevancyLexMetric"
"metric.lex.context_precision_unranked" = "evalguard_evaluators.metrics.rag:ContextPrecisionLexUnrankedMetric"
"metric.lex.context_recall"             = "evalguard_evaluators.metrics.rag:ContextRecallLexMetric"
"judge.pointwise"          = "evalguard_evaluators.judges.pointwise:PointwiseJudge"
"judge.mock_pointwise"     = "evalguard_evaluators.judges.pointwise:MockPointwiseJudge"
"guardrail.mock"           = "evalguard_evaluators.guardrails.mock:MockGuardrail"

[project.entry-points."evalguard.providers"]
"openai" = "evalguard_evaluators.providers.openai_provider:OpenAIProvider"
"ollama" = "evalguard_evaluators.providers.ollama_provider:OllamaProvider"
"mock" = "evalguard_evaluators.providers.mock_provider:MockProvider"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["evalguard_evaluators"]
