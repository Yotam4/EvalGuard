[project]
name = "evalguard-workspace"
version = "0.0.0"
description = "EvalGuard monorepo workspace root."
requires-python = ">=3.11"

[tool.uv.workspace]
members = ["packages/cli", "packages/evaluators"]

[tool.uv.sources]
evalguard = { workspace = true }
evalguard-evaluators = { workspace = true }
